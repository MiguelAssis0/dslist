spring:
  application:
    name: dslist
  profiles:
    active: ${APP.PROFILE:test}
  jpa:
    open-in-view: false

cors:
  origins: ${CORS_ORIGINS:http://localhost:5173, http://localhost:3000}

server:
  error:
    include-stacktrace: never
